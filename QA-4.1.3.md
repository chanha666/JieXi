# 4.1.3 媒体任务修复验收记录

日期：2026-09-07。以下结果分层记录，不能替换成“所有功能已测试通过”。

## 自动测试

- sharedCore：18 项，0 失败、0 跳过。
- Windows：109 项，0 失败；104 通过，5 项默认显式跳过（真实认证、在线更新、3 项公开视频网络检查）。
- Android JVM：82 项，0 失败、0 跳过。
- Android lint、调试 APK、设备测试 APK：构建检查通过；设备测试 APK 构建不等于在设备上执行过。

新增/强化覆盖：

1. 真实 yt-dlp 从本地 HTTP 服务下载时必须产生 `[progress]`，不再只核对文件存在。
2. 源站无响应时媒体任务变为失败，并保留工作目录。
3. 无响应期间暂停、取消优先于超时错误。
4. 单独重试和全部重试均能恢复经过 ffprobe 检查的已有文件，源站请求数为零。
5. 无下载核心不能伪造 YouTube 解析成功；画质预设不能超过返回格式的最高分辨率。
6. 文件实际增长才延长等待，重试日志不延长等待；合并宽限有上限。
7. Android 排除字幕、封面、空文件和未合并轨道；正确接受最终 MP4/WebM/MP3，多个候选明确失败。

## Windows 真实网络检查（显式开启）

使用用户反馈的 YouTube 视频 `JXZ_CUfTweo`，未导入 Cookie，未改动系统代理，未操作原下载任务。

- 真实解析：返回英文视频标题和 2160p 格式，通过。
- 同视频低流量格式完整下载：17,167,137 字节；ffprobe 返回视频和音频两个流，通过。
- 4K 选项：实际收到 261,120 字节并出现进度/速度，主动停止专用 QA 进程。只算传输启动通过，不算整部 4K 下载通过。
- 辅助媒体直链 Range 检查：HTTP 206，1024 字节，通过。
- 早期使用的历史短测试视频 `BaW_jenozKc` 已不可用，记录为样本不可用；随后使用用户实际反馈视频完成下载，不把这个失败隐藏成通过。

测试生成文件位于 `D:\CodexBuilds\JieXi\media-live-qa`，不写入用户正式下载目录。

## Android API 35 模拟器实测

- 设备：D 盘新安装模拟器，JieXiTestApi35 / emulator-5556，独立包名 com.yunx.app.qa。
- 本地 HTTP 样本：真实核心解析、7482 字节逐字节比对、9 次回调，通过。
- 用户 YouTube 视频：未登录，借助 adb reverse 临时连接电脑现有代理，返回真实标题及 2160p，通过。
- 同视频低流量格式完整下载、音视频合并：17,167,085 字节、3908 次回调，通过。没有做完整解码或真实手机 UI/前台服务全链路验收。
- 模拟器 crash 日志检查为空；不代表所有页面和设备均无崩溃。
- 首次测试脚本空代理值导致参数歧义，改成 --proxy= 后重测通过。早期串行分片完整下载测试主动中止，改用正式版相同的 8 路分片后完整通过。

## 未完成/不能扩大解释的部分

- Android 实体手机未连接；已完成上述模拟器核心运行验证，不等于实体手机和 UI/下载服务全流程均验收。
- 尚未逐一用真实账号验证全部网盘；没有替用户进行远端删除、转存、分享等操作。
- 抖音、X、B站的现有自动回归测试通过，不等于本次重新逐站完成真实视频下载。
- Windows 整部 4K/8K、Android 各品牌电池后台策略、断网后跨网络恢复，不在本次已通过清单内。

## 设备验证入口

调试版使用独立包名 `com.yunx.app.qa`，不会覆盖正式版账号和任务。安装调试 APK 及 androidTest APK 后，在选定测试设备运行：

```text
adb -s DEVICE_SERIAL shell am instrument -w com.yunx.app.qa.test/com.fuke.mobile.MediaSmokeInstrumentation
```

成功应返回 `MEDIA_SMOKE_OK`，并分别列出真实下载核心初始化/JSON 解析、文件字节比对和进度回调检查。传入 `-e live 1` 额外请求真实 YouTube；增加 `-e download 1` 完整下载低流量格式；可用 `-e proxy URL` 为测试指定代理。测试完成后移除专用 adb reverse 端口。

## 发布策略

4.1.3 经用户确认发布 GitHub 并更新签名在线更新清单。上述核心测试已通过，仍需区分实体手机、UI/下载服务全流程及其他平台的未测范围。发布授权不代表未测项目已经通过。
