# 安全策略

## 支持版本

只为最新正式版提供安全修复。请先在软件“设置 → 检查更新”升级，再复现问题。

## 报告安全问题

不要在公开 Issue 中提交 Cookie、Token、完整分享链接、私人文件名或诊断原始日志。请发送邮件至 `3316109338@qq.com`，包含版本号、Windows 版本、复现步骤和“导出脱敏诊断包”生成的 ZIP。

项目不会索取网盘密码。凭据仅由用户在网盘官方页面完成登录后导入，并使用当前 Windows 用户的 DPAPI 加密保存。

## 发布验证

正式 Release 必须同时提供 SHA-256 校验文件。应用内更新只接受受信 RSA 私钥签名的 HTTPS 清单，并在运行安装包前再次核对 SHA-256。
