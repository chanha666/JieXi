# 解析 3.0 使用说明

## Windows

安装版双击 `解析-3.0.2-安装版.exe`，完成安装后从桌面打开“解析”。绿色版进入 `绿色版` 后双击 `解析.exe`。

1. 打开“解析”，粘贴网盘分享链接，提取码有就填、没有留空。
2. 能匿名解析时直接显示文件；服务端要求身份时，再到“云盘登录”登录对应平台。
3. 点文件右侧“下载”，任务会进入“下载”页。
4. 暂停后可继续；程序异常关闭后重新打开，任务显示“可恢复”。
5. “平台状态”只检查网络入口和本机账号配置；真实下载是否可用仍以实际分享测试为准。
6. 在“设置”可进入显示主题、诊断中心、诊断日志和检查更新。GitHub 反馈需先填写你自己的 `https://github.com/用户名/仓库名`。

## Android

安装 `解析-3.0.2-Android.apk`。若系统阻止安装，需由用户在系统设置中允许当前文件管理器安装未知应用。

底部依次为解析、网盘、下载、记录、诊断和设置。通知栏可暂停、继续或取消当前任务。默认使用“单任务满速”；需要同时下载多个文件时再改为“均衡”或“极速”。

## 校验文件

发布目录中的 `SHA256SUMS.txt` 用来确认文件未被替换。对安装包计算 SHA-256 后，应与其中对应值完全一致。
