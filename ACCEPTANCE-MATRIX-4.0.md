# 解析 4.0 验收矩阵

更新时间：2026-09-04
适用范围：当前工作区的 4.0.0 发布候选，而不是尚未创建的公开 `v4.0.0` Release。

## 判定规则

- **通过**：本轮已有可复现的自动测试、实际运行或产物验证证据。
- **受限通过**：目标路径已验证，但仍存在明确的架构、环境或站点限制。
- **条件未满足**：缺少合法账号、公开样本、网络或设备条件，未进行真实验收。
- **待验**：实现或脚本已存在，但最终发布产物或端到端流程尚未验证。
- **未通过**：实际执行已经失败，不能用“理论支持”或“能识别网址”替代。

“构建通过”只证明源码在该构建环境中通过，不能证明第三方平台真实下载、登录状态、下载速度或长期兼容性。

## 自动化与构建

| 项目 | 结果 | 当前证据 | 发布前动作 |
| --- | --- | --- | --- |
| 共用链接分类器测试 | 通过 | `sharedCore` 当前报告 6 项测试，0 失败、0 错误 | 在最终干净提交重新执行 |
| Windows 桌面单元测试 | 通过 | `:desktopApp:test --rerun-tasks`：22 个套件、74 项测试、0 失败、0 错误；1 项需要真实在线网盘凭据的测试跳过 | 最终标签构建再次执行并归档报告 |
| Android 与共享核心单元测试 | 通过 | `:app:testDebugUnitTest`：78/78；`:sharedCore:test`：6/6；均为 0 失败、0 错误、0 跳过 | 最终标签构建再次执行并归档报告 |
| Android Lint | 通过 | `:app:lintDebug`：0 Error/Fatal、87 Warning、17 Hint；不把“0 阻断错误”写成“0 问题” | 正式发布前复核 Error/Fatal 仍为 0 |
| Windows 绿色版 | 通过 | 候选应用目录已从 D 盘实际启动，媒体组件清单复核无不匹配 | 生成正式 ZIP 后以 `SHA256SUMS.txt` 记录哈希，并重新启动验证 |
| Windows 安装版 | 受限通过 | 候选 Setup EXE 已成功生成；候选大小与哈希不作为最终发布值 | 使用最终 Setup EXE 实测安装、覆盖升级、启动和卸载；哈希以 `SHA256SUMS.txt` 为准 |
| Android Release APK | 通过 | `:app:verifyAndroidRelease` 成功；普通公开 APK 为新 RSA 4096 位发行证书 | 最终 APK 重新验签，大小与哈希以 `SHA256SUMS.txt` 和 `BUILD-INFO.txt` 为准 |
| GitHub Actions CI | 待验 | CI 已配置 Windows 测试/绿色版和 Android 测试/Lint/Debug APK；配置文件存在不等于远端运行成功 | 推送最终提交后确认两个 job 绿色 |
| CodeQL | 待验 | 工作流已配置 Java/Kotlin 分析；尚无当前 4.0.0 提交的远端成功记录 | 确认当前提交分析成功且无阻断告警 |

## Android 实际运行

| 场景 | 结果 | 当前证据 | 未覆盖范围 |
| --- | --- | --- | --- |
| Android 15 冷启动 | 通过 | `emulator-5554`、API 35、x86_64；已完成冷启动，未发现 `AndroidRuntime`/`FATAL` | Android 24–34 真机、不同厂商后台策略 |
| 本地 HTTP MP4 直链 | 通过 | 完成约 15 MB 文件解析与下载，进度从 2/15、3/15、5/15 MB 持续变化并最终完成 | 公网速度与 CDN 行为 |
| 暂停与继续 | 通过 | 下载到约 2 MB 后暂停，字节数停止；继续后完成 | 强制结束进程后的断点恢复仍需真机复核 |
| MediaStore 保存 | 通过 | 成品写入系统 `Download/解析`，重复下载生成 `(1)` 文件，不覆盖原文件 | SD 卡、目录权限撤销、极低空间 |
| 媒体核心初始化 | 通过 | youtube-dl-android、FFmpeg、Aria2 完整初始化成功；使用本地 404 URL 验证错误路径未导致 native crash | 仅证明组件可加载，不证明站点兼容 |
| 新发行证书安装与迁移 | 受限通过 | 普通公开 APK 使用新 RSA 4096 位证书；API 24–32 的旧 debug 测试版不能覆盖，需卸载一次。API 33+ 仅提供单独迁移 APK 的保数据路径 | 最终公开 APK 做全新安装与同证书后续覆盖；单独验收 API 33+ 迁移包，且不把它上传为普通公开附件 |
| 密码与提取码隐私 | 通过 | 密码/短信码/提取码不进入 SavedState；迅雷临时密码在成功、离开和登出时清空；收藏与分享不持久化 `pwd`，对应测试通过 | 真机进程备份/恢复的人工抽查 |
| 网盘与媒体统一入口 | 受限通过 | 同一应用和导航可进入两类任务 | **底层仍是两个独立队列**：网盘 DownloadManager 与媒体前台 Service 各自持久化和调度，不是统一底层任务队列 |
| 图片局部修复 | 受限通过 | 功能与本地处理路径存在，不上传源图，不覆盖原图 | 属于框选/涂抹后的本地像素修复，不是自动识别水印或生成式 AI；复杂背景效果需人工样本复核 |
| 后台、通知操作与进程恢复 | 待验 | 前台服务、通知动作和遗留状态恢复逻辑已实现 | 仍需真机逐项执行后台、锁屏、强制结束、重启、暂停/继续/取消 |
| 小屏、横屏、平板、字体放大 | 待验 | 响应式实现存在 | 需至少一台小屏真机、横屏、平板/大屏和 200% 字体人工检查 |

## Windows 实际运行

| 场景 | 结果 | 当前证据 | 未覆盖范围 |
| --- | --- | --- | --- |
| 应用启动与主要页面 | 受限通过 | 绿色版从 D 盘实际启动；窗口、标题栏和侧栏显示新图标，媒体工具页正确显示三项引擎版本 | 仍需全页、长时间运行和异常恢复复核 |
| 媒体引擎随包 | 通过 | 绿色版含 yt-dlp、FFmpeg、ffprobe、Deno、许可证、SBOM 和第三方声明；`app/vendor` 共 22 个文件，`MANIFEST.sha256` 21 项、0 不匹配 | 正式 ZIP 生成后再次复核 |
| 图片局部修复 | 受限通过 | 框选映射和局部修复自动测试通过，保存逻辑不覆盖原图 | 需要最终绿色版手工导入、框选、保存、打开结果文件 |
| 本地 HTTP MP4 直链 | 通过 | `DesktopMediaEngineTest` 使用捆绑 yt-dlp 完成真实下载，结果与源文件逐字节一致且无 `.part` 残留 | 公网站点、断线恢复和大文件仍需回归 |
| FFmpeg 与 ffprobe | 通过 | 捆绑 FFmpeg 真实生成 MP4，ffprobe 验证同时存在 video/audio 轨道；候选媒体引擎测试已通过 | 最终干净提交全量重跑；UI 中音频提取、截图和信息导出仍需人工逐项打开成品 |
| 音频提取、截图、媒体信息 UI | 待验 | 引擎底层已通过真实媒体测试，界面与调用路径已实现 | 需在最终绿色版逐项生成并打开结果 |
| 1366×768 与 125%/150% 缩放 | 待验 | 滚动和缩放适配代码存在 | 最终候选包需逐页检查无遮挡、无截断、可键盘操作 |
| 安装、覆盖升级与卸载 | 待验 | 安装脚本存在 | 必须使用最终 Setup EXE 实际执行三条路径 |
| Authenticode | 未通过 | 当前安装 EXE 检查结果为 `NotSigned` | 正式说明继续标注“未知发布者”，或取得证书后重签并验证 |

## 真实平台与账号验收

| 平台/类型 | 结果 | 当前证据 | 结论 |
| --- | --- | --- | --- |
| 普通 MP4 直链 | 通过 | Android 本地 HTTP 直链完成下载并核对成品文件落盘 | 只覆盖直链路径，不代表页面站点解析或全部播放器兼容性 |
| YouTube 匿名页面 | 未通过 | 匿名实测受到 YouTube 站点挑战，未形成可下载成品 | 不得宣传“无需登录稳定下载 YouTube”；可使用用户合法 Cookie 重新验收 |
| 哔哩哔哩公开页面 | 受限通过 | 官方匿名接口在宿主机完成 51,973,319 字节 720p 成品下载；ffprobe 验证 H.264 1280×720 + AAC。Android 模拟器一次解析耗时 1.615 秒 | 连续压力请求可能触发原站 HTTP 403；Android 尚需完成成品下载与 MediaStore 可播放复核 |
| 抖音 | 受限通过 | Android 完成 148,915,518 字节公开样本下载并落盘；Windows 完成同类页面解析，Range 请求返回 206 | Windows 尚需完成整个成品下载；站点接口和 CDN 断流行为可能变化 |
| X | 通过 | 公开样本完成 16,608,450 字节下载；ffprobe 验证 H.264 1280×720 + AAC，时长约 105.10 秒 | 单个公开样本不能证明所有帖子、地区和长期兼容性 |
| TikTok、小红书、微博、AcFun、视频号 | 待验 | 可交由 yt-dlp 或站点适配器处理，但本轮没有逐站成品证据 | 未实测平台不得标成通过 |
| 4K/8K | 条件未满足 | 代码允许读取站点实际开放格式 | 缺少本轮真实 4K/8K 样本成品；仅在源站实际开放时可用 |
| 夸克、UC、迅雷、百度、123、139 | 条件未满足 | 缺少六个平台全部有效账号和可公开测试样本 | 不能声明六平台真实下载全部通过 |
| DRM、会员、付费内容 | 不在范围 | 产品明确不绕过访问控制 | 不做绕过验收 |

## 更新、安全与发布

| 项目 | 结果 | 当前证据 | 发布前动作 |
| --- | --- | --- | --- |
| Android APK 发行签名 | 通过 | 普通公开 APK 在所有受支持系统上使用新的 RSA 4096 位发行证书，证书 SHA-256 `efca75197d508261f0f09e620ee6ebd706ecb2b7d30cc430679c24fbbef9cdd4`；API 24–32 不继承旧 debug 签名，API 33+ 的旧数据迁移使用单独迁移 APK | 对最终公开 APK 再次输出并归档证书指纹；迁移 APK 不作为普通公开附件 |
| Android 更新文件 SHA-256 | 通过 | 预期哈希进入下载任务，发布前流式校验；不匹配时删除文件，回归测试覆盖 | 用篡改的实际 APK 再做一次人工拒绝验证 |
| Windows 更新清单验签 | 通过 | 单元测试覆盖 RSA 验签、版本比较和篡改拒绝 | 最终 v4 清单必须用作者私钥重新签名 |
| 高置信密钥静态扫描 | 通过 | 当前源码审计未发现提交的私钥、真实密码或可直接使用的高置信 Token | 发布前对干净提交和打包目录各扫描一次 |
| 最终发布目录 | 待验 | `tools/BuildRelease.ps1` 定义统一输出，但当前正式 `release` 目录尚未完成 | 从干净提交生成 Setup、Portable ZIP、Android APK、`SHA256SUMS.txt`、`BUILD-INFO.txt` 五个公开附件 |
| `SHA256SUMS.txt` | 待验 | 生成逻辑存在 | 对最终附件重算并独立复核 |
| `update-manifest.json` 4.0 | 待验 | 签名脚本已定义三个 4.0.0 资产 URL；仓库现有清单解码后仍为 `v3.1.0`，仅含旧 Setup EXE | 发布附件后生成、验签并检查 URL/哈希完全匹配 |
| GitHub `v4.0.0` Release | 待验 | 尚未以最终干净提交和最终附件完成公开发布 | 创建 tag/Release，上传附件，核对下载与哈希 |
| Windows 应用内升级 | 待验 | 更新客户端与签名校验测试通过 | 从公开旧版执行检查、下载、校验、退出安装、启动新版 |
| Android 应用内升级 | 待验 | 更新下载、哈希和发行证书预检已实现；普通公开 APK 不兼容 API 24–32 旧 debug 测试版覆盖 | 验证公开 APK 全新安装和今后同证书覆盖；另测 API 33+ 单独迁移 APK。API 24–32 旧 debug 测试版按卸载后重装处理 |
| 源码、标签与产物一致 | 待验 | 当前工作区仍有未提交修改和未跟踪文件 | 仅从最终干净提交构建，并在 `BUILD-INFO.txt` 固定提交 SHA |

## 当前结论

当前候选已经证明两端核心可以构建，Android 直链下载主路径可工作，抖音、X 和哔哩哔哩取得了上述限定范围内的真实证据，Android 78/78 测试及 Lint 0 Error/Fatal 已通过；但它仍没有达到“所有平台真实下载全部通过”或“公开在线更新已经可用”的证据标准。

正式发布前的阻断项是：最终干净构建与哈希、Windows 最终安装/运行工具回归、Android 真机后台与通知回归、公开 `v4.0.0` Release、双端应用内升级，以及具备合法账号/样本的平台实际下载回归。没有证据的项目必须继续保留“待验”“条件未满足”或“未通过”。
