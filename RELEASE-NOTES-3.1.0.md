# 解析 3.1.0 Windows 正式版

本版把工程和发布流程彻底收敛为 Windows-only，并补齐成熟下载工具需要的系统集成与诊断能力。

## 新增

- 单实例保护，避免两个进程并发修改任务状态。
- 系统托盘、任务完成/失败通知、下载期间阻止系统休眠。
- 跟随系统、直连、HTTP 和 SOCKS5 四种代理模式。
- 一键生成脱敏诊断 ZIP，只保留排障必要状态。
- 启动后自动检查作者签名更新，仍支持设置页手动检查。
- 下载卡片显示结构化错误代码和自动重试次数。

## 工程与发布

- 删除 Android 模块、Android CI、调试密钥和相关构建要求。
- 工程名统一为 `JieXi`，版本统一为 `3.1.0`。
- GitHub Actions 改为 Windows `main` 分支测试和绿色版构建。
- 增加 `SECURITY.md`、`CONTRIBUTING.md`、更新记录和 Windows 使用说明。

## 安全边界

- 正式更新清单继续使用 RSA 签名，更新文件必须通过 SHA-256 校验。
- 网盘凭据继续由 Windows DPAPI 加密，仅当前 Windows 用户可以解密。
- 诊断包主动清除 Cookie、Token、密码字段和 URL 查询参数。
- Windows 安装包当前尚未配置商业 Authenticode 证书，因此新设备仍可能显示“未知发布者”。
