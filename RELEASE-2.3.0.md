# 解析 2.3.0 安全修复测试版

## 本版修复

- Android 下载引擎改为应用进程唯一实例，旋转屏幕或 Activity 重建不再产生失控的第二套下载任务。
- Windows 内置登录改用随机临时 Cookie 文件，导入 DPAPI 后立即覆写并删除；启动时自动清理旧残留，删除失败则拒绝继续。
- Android 与 Windows 内置登录只允许对应网盘官方 HTTPS 域名，离开允许域时交给系统浏览器。
- Android 退出登录只清理当前网盘域的 Cookie，不再误清除其他网盘登录态。
- Android 使用独立 4096 位 RSA 正式发行证书签名，不再使用仓库内调试证书。
- 在线更新只接受作者 RSA 私钥签名的清单；未配置自有 HTTPS 发布地址时安全关闭，不再读取原 YunX 仓库或第三方镜像。
- 崩溃报告与下载异常日志增加 URL、Cookie、Token 脱敏；崩溃报告上限 64 KiB，最多保留 5 份。

## 安装提示

- Windows：运行 `解析-2.3.0-安装版.exe`，按安装向导完成安装。
- Android：安装 `解析-2.3.0-Android.apk`。
- 由于 2.3.0 首次切换为正式证书，旧的调试签名 Android 版本无法直接覆盖安装。请先在旧版内导出网盘认证，再卸载旧版、安装 2.3.0 并导入认证。

## 已知限制

- 本版是 2.3 安全修复测试版，不包含 2.4 计划中的 Windows 持久队列和 Android 进程死亡自动续传。
- 在线更新地址尚未发布，因此本版不会显示在线更新；这是安全关闭，不是检测故障。
- Windows 安装包尚未配置商业 Authenticode 代码签名证书，首次运行可能出现 SmartScreen 提示。

## 密钥

- Android 发行密钥与更新私钥仅保存在本机 `D:\CodexSecrets\解析`，未包含在源码或成品目录中。
- 必须把该目录加密备份到另一块可靠介质。丢失 Android 发行密钥后，将无法为已安装用户提供覆盖升级。
