# 解析 4.0.0 整合版发布说明

> 状态：发布候选说明。只有完成正式构建、生成并复核哈希、发布 GitHub Release、验证应用内升级后，才能把本文件随 `v4.0.0` 标记为正式发布。

4.0.0 把 Windows 网盘下载器与 Android、Windows 公共媒体下载能力整合到同一品牌、同一仓库和同一更新通道。此版本强调本机处理、明确的权限边界、可恢复下载、诊断与发布可验证性。

## 计划发布附件

正式 `v4.0.0` Release 应同时提供以下文件，缺少任意一项都不算完整发布：

- `JieXi-4.0.0-Windows-Setup.exe`
- `JieXi-4.0.0-Windows-Portable.zip`
- `JieXi-4.0.0-Android.apk`
- `SHA256SUMS.txt`
- `BUILD-INFO.txt`

附件名必须与 `tools/BuildRelease.ps1`、`tools/SignUpdateManifest.ps1`、README 和应用内更新清单保持完全一致。发布前必须从干净 Git 提交重新构建；本地临时包和旧版 `update-manifest.json` 不能直接作为 4.0.0 正式附件。

## 主要变化

### Windows

- 增加公开媒体下载入口、批量媒体队列、格式与画质选项。
- 随包提供 yt-dlp、FFmpeg、ffprobe 与 Deno，并对媒体引擎文件进行 SHA-256 校验。
- 下载页同时显示网盘和媒体任务，并保留暂停、继续、重试、取消与删除操作。
- 工具页增加图片局部修复、MP3 提取、视频截图和媒体信息导出。
- 保留软件内网盘登录、DPAPI 凭据保护、诊断包脱敏、代理、托盘和应用内更新。
- 默认下载目录优先使用 `D:\解析下载`；D 盘不可用时使用用户下载目录，并允许在设置中修改。
- 程序文件与用户数据分离：4.0.0 的桌面端状态默认写入 `%LOCALAPPDATA%\JieXi\Data`，卸载程序不会把该目录当作安装文件清理。
- Windows 安装器只允许 3.1.0 及以上版本直接升级。在删除旧版之前，它仅迁移 `%LOCALAPPDATA%\解析` 中的 `state-v3.bin`、`state-v3.bin.bak` 和 `media-tasks-v1.json`，不复制其他文件、不覆盖已有新数据；占用、冲突、校验失败或异常路径都会中止升级并保留旧版。
- 3.0.x 及更早版本会被安装器安全阻止直接升级。用户必须先备份旧数据，卸载旧版，再全新安装 4.0.0。

### Android

- 恢复 Android 原生应用，统一名称、图标、反馈、赞赏和 GitHub 更新通道。
- 修复媒体下载初始化崩溃、进度不刷新、暂停/继续与后台前台服务协作问题。
- 对公开媒体直链提供快速下载，对页面链接使用本地 youtube-dl-android、FFmpeg 与 Aria2。
- 下载完成后通过 MediaStore 写入系统 `Download/解析`，同名文件自动避让，不覆盖旧文件。
- 增加图片局部修复、音频提取、视频截图和系统分享入口。
- 更新包下载完成后校验签名清单中的 SHA-256；普通公开 APK 使用新的 RSA 4096 位固定发行证书。
- 密码、短信验证码和网盘提取码不写入页面恢复状态；收藏和分享文本不持久化提取码。

普通公开 APK 不继承早期 debug 测试版的签名。Android 7–12（API 24–32）上的旧测试版需卸载一次后再安装；Android 13 及以上（API 33+）如需保留旧数据，使用单独的迁移 APK。迁移 APK 不作为普通公开 Release 附件。

## 已完成的本机证据

截至 2026-09-04，已经取得的本机证据包括：

- Android 15 x86_64 模拟器完成安装、冷启动和主要页面检查，未发现 `AndroidRuntime` 或 `FATAL` 崩溃输出。
- Android 使用本地 HTTP 直链完成约 15 MB MP4 的解析、进度更新、暂停、继续、下载完成与 MediaStore 保存；重复下载生成不同文件名。
- Android 媒体核心完成 youtube-dl-android、FFmpeg、Aria2 初始化；该项只证明组件能加载，不等于所有网站均已实际下载通过。
- Android JVM 测试 78/78 通过，0 失败、0 错误；共享链接核心 6/6 通过；Lint 为 0 Error/Fatal、87 Warning、17 Hint。不能把“0 阻断错误”写成“Lint 零问题”。
- Android 普通公开候选 APK 已验证为新 RSA 4096 位发行证书。API 24–32 的旧 debug 测试版不能直接覆盖；API 33+ 只通过单独迁移 APK 保留旧数据，不把迁移包作为普通公开附件。
- 抖音公开样本已在 Android 完成 148,915,518 字节成品下载并核对完成状态；同一样本在 Windows 完成官方页面解析，媒体地址的 Range 请求返回 206。首次 CDN 连接中途结束后，续传逻辑从已有进度恢复并完成。
- X 公开样本已完成 16,608,450 字节成品下载；ffprobe 验证为 H.264 1280×720 视频、AAC 音频，时长约 105.10 秒。
- 哔哩哔哩公开样本已通过官方匿名接口在宿主机完成 51,973,319 字节 720p 成品下载；ffprobe 验证为 H.264 1280×720 视频、AAC 音频。Android 模拟器一次解析在 1.615 秒内完成，但连续压力请求可能被原站以 HTTP 403 限制。
- Windows 候选绿色版已从 D 盘实际启动，媒体组件清单复核无不匹配。桌面端强制全量重跑为 81 项测试、0 失败、0 错误、1 项因无真实在线网盘凭据跳过；安装迁移助手 12/12 通过。Android JVM 测试 78/78、共享核心 6/6 通过；正式标签构建还会再次执行同一验收门。
- 这里不再记录候选安装包或 APK 的临时大小与哈希。正式发布的唯一依据是 Release 中的 `SHA256SUMS.txt` 和记录提交、工具链与构建时间的 `BUILD-INFO.txt`。

## 必须明确的能力边界

### Android 目前不是单一底层队列

Android 已把网盘与公开视频入口整合在同一个应用和导航中，但网盘下载管理器与媒体前台服务仍是两个独立的底层队列，使用各自的任务持久化和执行逻辑。4.0.0 不能把它宣传为“所有任务共用一个底层调度器”。

### 图片“去水印”是本地局部修复

图片功能由用户框选或涂抹区域后在本机做局部像素修复，输出一个新文件且不覆盖原图。它不是自动识别水印、不是生成式 AI 修复，也不保证复杂背景能够无痕恢复。

### 公开视频受原站限制

- YouTube 匿名实测遇到站点挑战，当前不能计为匿名下载通过；部分内容需要浏览器 Cookie、登录或站点验证。
- 抖音已取得 Android 完整下载证据和 Windows 解析/Range 证据；X 已取得完整可播放成品证据；哔哩哔哩已取得官方匿名 720p 完整下载证据和 Android 模拟器解析证据，但压力请求可能触发 403。这些是指定公开样本在本轮环境中的结果，不保证原站长期不变。
- TikTok、小红书、微博、AcFun、视频号等其余站点适配会随原站接口和反自动化策略变化。未取得实际“解析 → 下载 → 可播放”证据的平台不得写成已通过。
- 4K/8K 仅在原站向当前匿名状态或用户合法账号实际开放对应格式时可选；软件不会把低分辨率素材变成真正的 4K/8K。
- 软件不绕过 DRM、会员权益、付费墙、验证码或平台访问控制。

### 网盘仍需真实账号与样本验收

夸克、UC、迅雷、百度、123 和中国移动云盘（139）的代码与入口存在，但本轮没有六个平台全部有效账号及公开样本，不能声明“六平台真实下载全部通过”。正式发布前仍需逐个平台验证登录、目录、下载、断网恢复、直链过期和删除语义。

## 更新与安全

- Windows 登录凭据使用当前用户 DPAPI；Android 登录凭据使用 Android Keystore。
- 两端更新逻辑使用 HTTPS、RSA 签名清单和文件 SHA-256；正式发布仍必须验证公开更新清单、附件 URL 与下载文件完全匹配。
- Windows 安装包当前 Authenticode 状态为 `NotSigned`，首次下载可能显示“未知发布者”。RSA 更新清单签名不能替代 Windows 代码签名。
- Android 普通公开 APK 已使用作者固定的 RSA 4096 位发行证书签名；这不能改变 Windows 安装包未签名的状态。
- 诊断包和日志必须清除 Cookie、Token、密码、提取码及私人 URL 查询参数。

## 正式发布前剩余门槛

- 从干净 Git 提交生成安装版、绿色版、Android APK、`BUILD-INFO.txt` 和 `SHA256SUMS.txt`。
- 复核最终 Windows 与 Android 产物的哈希、签名、版本号、包名、可启动性和安装升级；所有值以该次构建生成的 `SHA256SUMS.txt` 与 `BUILD-INFO.txt` 为准。
- 建立公开 `v4.0.0` GitHub Release，并把相同的已签名更新清单部署到两端读取的正式地址。
- 从已发布的 Windows 3.1.0 或更高版本执行一次“检查更新 → 下载 → 校验 → 白名单数据迁移 → 安装 → 启动新版”端到端验收；另验证 3.0.x 会在不删除旧版和旧数据的前提下被阻止，并按“备份 → 卸载 → 全新安装”处理。Android 分别验证公开 APK 的全新安装/同证书后续覆盖升级，以及 API 33+ 单独迁移 APK 路径。API 24–32 的旧 debug 测试版明确按卸载后重装处理。
- 使用合法账号和可公开测试样本完成尚未具备条件的平台回归；失败或未测平台继续标记为实验或待验。

完整逐项结果见 [4.0 验收矩阵](ACCEPTANCE-MATRIX-4.0.md)。
