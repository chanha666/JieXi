# 解析 3.0.2 完整版

## 主要变化

- 全新暖白与微橘色界面；Windows 桌面端采用清晰的工作台侧栏布局。
- 下载任务持久化；异常退出后的运行任务转为“可恢复”，不再伪装成仍在下载。
- 批量暂停、批量继续、删除任务、手动重试、任务优先级与三档性能预设。
- 默认启用“单任务满速”：只运行一个下载任务，普通平台最多 48 路且不限速；迅雷保留 8 路安全上限。
- 自动错误分类、指数退避重试、分片续传、Range 不支持时单连接回退、完成大小校验。
- 解析历史与收藏；历史不保存提取码，Windows 状态文件使用当前 Windows 用户 DPAPI 加密并保留备份。
- 六平台状态页与诊断中心，分别显示网络、账号和适配等级，不把“网络正常”写成“解析成功”。
- 支持减少动画；主要按钮均有文本或无障碍描述。
- Windows 使用作者 RSA 签名更新清单；可在软件内检查更新、下载安装包并核对 SHA-256，校验失败自动删除。
- Windows 设置页新增 GitHub 问题反馈仓库并默认连接本仓库。
- Windows 设置页新增显示主题、诊断中心和诊断日志入口；日志按 1 MiB 自动轮换并保留 3 份。

## 重要边界

- “公开分享免登录”由各网盘服务端决定。软件不会也不能绕过服务端登录、风控、会员或访问控制。
- 六个平台目前统一标记为“实验”。在缺少用户授权的真实账号和真实分享样本时，不能诚实标记为“稳定”。
- Windows 安装包尚未进行商业代码签名，因此 Windows 可能显示未知发布者；这不影响本地 SHA-256 校验。
- 更新只信任构建时写入的 HTTPS 地址和作者 RSA 公钥；不会从 GitHub 搜索结果或原版项目自动替换定制版。

## 默认目录

- Windows：`D:\解析下载`（没有 D 盘时回退到当前用户 Downloads）。
