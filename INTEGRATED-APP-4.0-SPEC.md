# 解析 4.0 整合版实施规格

状态：实现与本机发布候选验收中；正式发布、公开升级链和缺少账号/样本的平台仍待验
目标：同一品牌、同一仓库、同一套能力边界下，提供 Windows 与 Android 两个原生安装包。

当前真实证据与未完成项目以 [4.0 验收矩阵](ACCEPTANCE-MATRIX-4.0.md) 为准。不得把构建成功、识别到网址或宿主机查询成功替代目标设备上的真实下载验收。

## 1. 产品边界

“解析”4.0 是本地下载与媒体处理工具，整合两类工作：

1. 网盘分享：夸克、UC、迅雷、百度、139、123 的分享识别、目录浏览、选择与下载。
2. 公开媒体：YouTube、哔哩哔哩、抖音、X、TikTok、小红书、微博、AcFun、直链视频及 yt-dlp 可公开提取的网站。

软件只下载公开资源或用户账号本身有权访问的资源，不绕过 DRM、会员权益、付费墙或平台访问控制。所谓“最高画质”指当前链接在匿名或已授权状态下实际返回的最高开放格式；若开放格式包含 4K/8K，则允许选择，不能凭空把低清视频变成 4K/8K。

## 2. 一体化体验

### 2.1 唯一入口

- 首页保留一个“粘贴链接或分享文案”输入区。
- 自动提取一条或多条 URL、网盘提取码及常见分享文案。
- `LinkClassifier` 将输入分为：网盘分享、视频页面、媒体直链、未知链接。
- 识别结果在提交前可见，用户可手动纠正。
- 系统分享、剪贴板建议和拖放文件均进入同一分类器。

### 2.2 信息架构

- 首页：统一输入、最近任务、平台状态和存储空间。
- 下载：网盘与媒体任务使用同一列表、筛选和批量操作。
- 网盘：账号、空间、目录与转存能力。
- 媒体：画质/格式、字幕、播放列表和公开原始视频流。
- 工具：图片去水印、音频提取、视频截图、封面保存、元数据导出。
- 记录：历史、收藏、重复任务检测和重新解析。
- 诊断：平台、网络、引擎、账号、存储、更新与脱敏日志。
- 设置：下载、网络、外观、隐私、安全、更新、反馈和赞赏。

### 2.3 渐进显示

- 常用路径只显示链接、识别结果和“解析”主按钮。
- 画质、容器、字幕、代理、线程与命名规则放在结果页或高级设置。
- 高频导航不使用拖慢操作的进场动画。
- 按压反馈 100–160 ms；弹窗/抽屉 180–260 ms；只动画 `transform` 与 `opacity`。
- Android 手势必须可中断；系统启用“减少动态效果”时改为短淡入淡出。

## 3. 共用领域模型

### 3.1 链接分类

```text
LinkKind = CLOUD_SHARE | VIDEO_PAGE | DIRECT_MEDIA | UNKNOWN
```

每个识别结果至少包含：标准化 URL、平台、能力类型、提取码、来源文本、是否可能需要登录、可信度和警告。

### 3.2 统一任务状态

```text
WAITING -> PREPARING -> RUNNING -> VERIFYING -> COMPLETED
                    |-> PAUSED
                    |-> RETRY_WAIT
                    |-> NEEDS_REAUTH
                    |-> NEEDS_INPUT
                    |-> INTERRUPTED
                    |-> FAILED
                    |-> CANCELLED
```

所有状态变化必须先持久化，再刷新界面。删除任务必须区分：仅删除记录、保留临时进度、连同本地文件删除。

### 3.3 能力声明

平台适配器必须报告：

- 可否匿名解析、是否需要登录、是否支持目录、是否支持断点续传。
- 实际最高分辨率、开放格式数、是否检测到 DRM。
- 适配器版本、最后成功时间、最近错误和当前支持等级。

界面不能把“识别到网址”显示成“已支持下载”，也不能把“存在凭据”显示成“登录有效”。

## 4. Windows 功能

- 复用现有网盘解析、DPAPI 凭据、批量队列、断点续传、代理、托盘、诊断与签名更新。
- 新增本机媒体引擎：yt-dlp、FFmpeg、ffprobe、Deno；可选 N_m3u8DL-RE。
- 媒体引擎包独立校验 SHA-256，可随安装包内置，也可从作者签名清单更新。
- 解析缓存复用 5 分钟，下载时避免再次访问站点。
- 支持自动最高开放画质、4K、1080p、720p、仅音频、自选格式、MP4/MKV、字幕和播放列表。
- 下载后使用 ffprobe 校验是否存在有效音视频轨道。
- 工具页支持音频提取、截图、封面、元数据和图片局部修复。
- 下载大文件默认落到 `D:\解析下载`；D 盘不可写时明确要求选择，不静默塞满 C 盘。

## 5. Android 功能

- 恢复 3.0.2 已有的网盘 Room 数据库、Application 级下载管理器、前台服务和系统目录授权。
- 复用“复刻”的 yt-dlp-android、FFmpeg、Aria2、抖音/X 快速兼容器和图片局部修复算法。
- 媒体服务属于 Application/前台 Service；Activity 重建不得创建第二下载实例。
- 通知栏提供暂停、继续和取消；进程重启后把遗留运行状态转换为 `INTERRUPTED` 并恢复。
- 支持 Android 分享菜单直接发送链接到“解析”，由统一分类器选择网盘或媒体流程。
- 支持仅 Wi‑Fi、漫游限制、低电量暂停、仅充电下载和通知权限引导。
- APK 使用作者固定发行签名；更新时核验包名、versionCode、ABI、SHA-256 和签名。

## 6. 从“复刻”迁移的能力

| 复刻现状 | 4.0 整合后 | 迁移原则 |
| --- | --- | --- |
| 单独的视频首页 | 统一链接入口 + 媒体结果页 | 复用解析参数，不复制第二套首页 |
| 单独任务 JSON/SharedPreferences | 统一持久任务模型 | 不保留两个互不认识的任务中心 |
| yt-dlp / FFmpeg / Aria2 | 媒体引擎适配器 | 引擎生命周期脱离页面 |
| 抖音、X 快速回退 | 可撤销的站点适配器 | 失败自动回退 yt-dlp，并标出来源 |
| 图片去水印 | 工具页“图片局部修复” | 保留框选与本机处理，不上传图片 |
| 音频、截图、封面、元数据 | 工具页 | 输入文件只在本机处理 |
| 暖白微橘视觉 | 全产品设计令牌 | 统一字号、间距、状态色和对比度 |
| 赞赏、邮箱反馈 | 设置/赞赏页 | 使用作者自己的二维码与 GitHub/QQ 邮箱 |

## 7. 仍需补齐的产品能力

### P0：不完成不能交付

- 两端统一分类器及系统分享路由。
- Android 媒体下载闪退、进度不动、任务恢复和通知操作修复。
- 两端统一删除语义、磁盘预检、文件冲突和完整性校验。
- 作者图标、反馈、赞赏资源、更新仓库与发行签名全部一致。
- 凭据、临时直链、日志和诊断包脱敏；第三方接口不得接收账号凭据。

### P1：4.0 必须完成

- 批量粘贴、批量队列、暂停/继续/重试/取消/删除。
- 画质和格式选择、字幕、音频提取、播放列表控制。
- 历史、收藏、重复任务提醒、失败原因与一键恢复动作。
- 平台/引擎状态中心、下载核心更新、应用内签名更新。
- Windows 小窗口滚动、四档缩放、键盘导航；Android 小屏、横屏、平板和字体放大。
- 一键导出脱敏诊断包，并自动附带版本、平台、引擎、网络和错误编号。

### P2：不阻断首个整合版

- 任务优先级拖动、定时下载和分时限速。
- 同局域网设备间发送链接（默认关闭，不同步 Cookie）。
- 插件化站点适配器的签名热更新与安全回滚。
- 多语言和可选便携媒体引擎包。

## 8. 安全、许可与防冒名

- 本项目继续遵守上游 GNU AGPL-3.0；开源软件无法从技术上绝对阻止别人修改名称。
- 官方发行包通过 Windows/Android 代码签名、软件内“官方版本校验”、GitHub 发布哈希和固定更新公钥识别。
- 关于页保留上游归属、作者仓库、版本签名指纹和许可证。
- 不把“禁止改名”伪装成无法绕过的技术保护；真正有效的是签名信任链、官方发布渠道和商标/许可证声明。
- 媒体引擎及插件随包附带各自许可证与来源版本，生成 SBOM 和第三方许可清单。

## 9. 验收门槛

### 自动化

- Windows 与 Android 所有现有单元测试通过。
- 新增分类器、任务恢复、删除语义、文件名安全、进度解析、日志脱敏和更新验签测试。
- Android Lint、Release APK、Windows 绿色版和安装版构建通过。

### 实际运行

- Windows 实际启动并逐页检查；1366×768、125%/150% 缩放不截断。
- Android 真机或模拟器完成冷启动、旋转、后台、强制结束、恢复和通知栏操作。
- 至少各验证一个公开网盘样本、公开视频样本、直链、失败链接和本地工具样本。
- 抖音/X 必须实际完成“解析 → 下载 → MediaStore 可播放”，不能只验证解析结果。

### 发布

- 生成 Windows 安装版、绿色版、Android Release APK、源码包和 `SHA256SUMS.txt`。
- 更新清单签名及包哈希核验通过。
- 记录所有未具备真实账号/样本的项目，并将对应平台标成“实验”，不得声称完整通过。

## 10. 实施顺序

1. 恢复 Android 网盘主模块并保持两端可编译。
2. 抽出统一链接分类器、平台能力和任务状态。
3. Android 接入媒体引擎与工具；修复生命周期和进度。
4. Windows 接入媒体引擎与工具。
5. 合并任务、记录、诊断、设置、反馈、赞赏和更新入口。
6. 统一视觉、图标、无障碍和减少动画。
7. 完成自动化、实际运行、安装升级与发布验收。
